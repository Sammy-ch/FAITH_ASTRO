---
export function getStaticPaths() {
    return [
        {params: {slug: 'hello_world'}},
        {params: {slug: 'my-favorite-things'}},
        {params: {slug: 'summertime'}},
    ];
}

const {slug} = Astro.params;

---


<h1>A post about {slug}</h1>